# 카드뉴스 디자인 시스템

`index.html`의 `buildCardMarkup()` 함수가 실제로 그리는 카드뉴스 슬라이드의 디자인 규칙을 정리한 문서입니다. **카드 디자인(색상·타이포그래피·레이아웃)을 수정할 때는 이 문서도 같이 업데이트해서, 코드와 문서가 항상 같은 내용을 말하도록 유지할 것.**

이 문서는 2026-09-10에 실제 생성된 카드 4장(표지·본문 2종·CTA)을 리뷰하고 발견된 문제를 고친 뒤 작성됐습니다.

## 카테고리 시스템 없음 (2026-09-10 제거)

예전엔 "개인회생"·"형사사건" 두 카테고리가 코드에 고정되어 있었고, 카테고리마다 별도 색상·AI 페르소나(광고 규정 문구)를 가질 수 있었음. 사용자가 개인회생·형사사건이라는 좁은 소재를 벗어나 "돈 안 잃는 법" 같은 더 넓은 금융 콘텐츠(돈 버는 법·사기 대처법 등)를 자유롭게 다루길 원해서 **카테고리 개념 자체를 완전히 제거**함:

- `state.pipeline`/`state.cardPipeline`에 더 이상 `category` 필드가 없음. `CATEGORY_LABEL`, `categoryPersonaPrompt(category)`도 삭제됨.
- AI 페르소나는 `writingPersonaPrompt()` 하나로 통일 — 특정 카테고리 규정이 아니라 "결과·수익 보장 표현 금지, 과장 광고 금지" 같은 범용 광고 규정 준수 원칙만 항상 적용하고, `state.styleGuide`(글쓰기 스타일 가이드)로 사용자가 원하는 톤·주제를 자유롭게 지정함.
- 색상도 카테고리별이 아니라 **단일 팔레트**(`state.cardColorPresets`, 카테고리 키 없이 flat)로 통일됨.
- 새 "테마" 개념을 다시 추가하고 싶어지면, 카테고리처럼 코드에 고정된 옵션을 또 만들지 말 것 — `styleGuide`/`cardNewsPrompt` 같은 자유 텍스트 필드로 해결하는 게 이 프로젝트의 방향과 맞음.

## 색상 토큰 (`defaultCardColors()`, index.html)

`state.cardColorPresets`는 카테고리 구분 없이 만드는 모든 카드뉴스에 공통으로 적용되는 단일 팔레트입니다. 기본값은 아래와 같습니다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `accent` | `#06C8E3` (Primary Accent Cyan) | 강조 바, 슬라이드 번호, 불릿 점, CTA 보조 텍스트 |
| `coverBg` / `ctaBg` | `#121C28` (Dark Navy) | 표지·CTA 슬라이드 배경 — 이 둘은 항상 같은 색이어야 함 (스토리의 시작·끝을 시각적으로 묶는 장치) |
| `coverText` / `ctaText` | `#ffffff` | 표지·CTA의 기본 글자색 |
| `contentBg` | `#ffffff` | 본문 슬라이드 배경 |
| `contentText` | `#121B27` | 본문 슬라이드 기본 글자색 |

보조(Secondary) 텍스트색은 고정값이 아니라 배경 밝기에 따라 자동 계산됩니다 (`isDarkColor(bg)` 기준): 어두운 배경이면 `rgba(255,255,255,0.68)`, 밝은 배경이면 `#515E73`.

## 타이포그래피

- 기본 폰트: **Pretendard** (`'Pretendard','Noto Sans KR',sans-serif`). 사용자가 레이어별로 다른 폰트로 바꿀 수 있지만 기본은 항상 Pretendard.
- 전화번호·연락처처럼 숫자 데이터에는 **IBM Plex Mono** (`'Pretendard','IBM Plex Mono',monospace`) — CTA 박스 아래 전화번호, 푸터 전화번호.
- 폰트 크기는 카드 가로폭(`w`, 기본 1080px) 기준 비율로 계산: 헤드라인은 표지 `w*0.075`, 본문 `w*0.058`, CTA `w*0.062`; 보조설명 `w*0.032`; 강조 문구(HIGHLIGHT) `w*0.038`(본문)/`w*0.042`(CTA 박스); 푸터 `w*0.026`.
- **모든 한글 텍스트 레이어에는 `word-break: keep-all`이 반드시 있어야 함.** 이게 없으면 브라우저가 음절 단위로 줄바꿈해서 "사라진다면?"이 "사라진 / 다면?"처럼 단어 중간에서 끊긴다 (2026-09-10에 발견하고 고친 버그 — 다시 생기지 않게 주의).

## 레이아웃

- 캔버스 기준 크기: **1080×1350px** (4:5, 인스타그램 카드뉴스 표준 비율).
- 안전 여백(pad): `w * 80/1080` (약 7.4%) — 상하좌우 공통.
- 콘텐츠 텍스트 박스 너비: `w * 0.84` (여백 제외 전체 너비의 84%).
- 콘텐츠 시작 위치(세로): 표지 `H*0.19`, 본문 `H*0.185`, CTA `H*0.30` — 이 지점부터 헤드라인이 시작됨.
- 본문 슬라이드는 헤드라인 위에 2자리 슬라이드 번호("01", "02"...)를 accent 색으로 고정 표시 (표지·CTA는 번호 없음).
- 보조설명이 2줄 이상이면 원형 점 리스트로 렌더링되고, 1줄이면 그냥 텍스트로 렌더링됨 — 이 분기 규칙은 유지할 것.

## 다중 항목 스타일 (2026-09-10 수정: 색 박스 → 구분선)

예전엔 보조설명이 2줄 이상이면 항상 색이 채워진 둥근 박스(배경 + 패딩)로 감쌌는데, 슬라이드 8장 중 대부분이 이 박스 형태가 되면서 덱 전체가 단조롭게 느껴진다는 피드백을 받고 바꿈. 지금은:

- 항목 사이에 **얇은 구분선만** 넣음 (마지막 항목 아래는 선 없음). 배경색·둥근 테두리·바깥 패딩 전부 없음.
- 구분선 색은 `isDarkColor(bg)` 기준으로 어두운 배경이면 `rgba(255,255,255,0.14)`, 밝은 배경이면 `rgba(18,28,40,0.1)`.
- 원형 점 불릿은 그대로 유지 (박스가 아니라 리스트 마커라서 문제 없음).
- 생성 프롬프트(`generateCardNewsScript`)에도 "모든 슬라이드를 항목 나열식으로 통일하지 말고, 한 문단으로 자연스럽게 풀어 쓰는 슬라이드도 섞어라"는 지침을 추가함 — 스타일뿐 아니라 콘텐츠 구조 자체도 슬라이드마다 다양하게 나오도록.
- **다시 색이 채워진 박스 스타일로 되돌리지 말 것** — 이건 사용자가 명시적으로 별로라고 한 부분임.

## CTA 슬라이드 규칙 (2026-09-10 수정)

CTA(마지막) 슬라이드는 **행동 유도 요소가 정확히 하나**여야 합니다:

- 흰 박스 안 문구 = `slide.highlight`(AI가 만든 강조 문구)가 있으면 그걸 쓰고, 없으면 브랜드 태그라인(`brand.tagline`)을 씀. 이게 유일한 "행동을 촉구하는" 문장.
- 박스 바로 아래 보조 줄 = **항상 전화번호**(`brand.phone`), accent색 모노스페이스. 절대로 태그라인이나 또 다른 CTA 문구를 넣지 말 것 — 박스 문구와 겹쳐서 "행동 유도가 두 개"처럼 보이는 게 2026-09-10에 발견된 버그였음.
- 푸터에 사무소명+전화번호가 또 나오는 건 의도된 반복(연락처 강화)이라 문제 없음 — 겹치면 안 되는 건 "행동 유도 문구" 두 개가 나란히 있는 경우뿐임.

## 세로 여백 규칙 (2026-09-10 수정)

예전엔 콘텐츠가 항상 카드 상단의 고정된 지점(`contentTop`)에서 시작해서, 텍스트가 적은 슬라이드는 카드 하단 40~60%가 그냥 비어보였음. 지금은 `computeInitialSlideLayout()`이 역할별로 "사용 가능한 밴드"(bandTop~bandBottom)를 정해두고, 그 안에서 헤드라인+본문 블록을 **세로로 중앙 정렬**함:

- 표지: bandTop `H*0.19` ~ bandBottom `H*0.78`
- 본문: bandTop `H*0.185` ~ bandBottom `H*0.75`
- CTA: bandTop `H*0.30` ~ bandBottom `H*0.57` (그 아래 흰 CTA 박스가 `h*0.62`에서 시작하므로 겹치지 않게 여유를 둠)

블록이 밴드보다 짧으면(텍스트 적음) 아래로 내려가 밴드 중앙에 오고, 블록이 밴드보다 길면(텍스트 많음) bandTop에서 그냥 시작함(넘치지 않게). **이 밴드 경계값을 벗어나게 하드코딩하지 말고, 새 역할(예: 새로운 슬라이드 타입)을 추가할 때도 반드시 bandTop/bandBottom 쌍으로 정의할 것.**

이 로직은 슬라이드를 **새로 생성할 때만** 적용됨 — 이미 저장된 프로젝트의 기존 슬라이드는 영향받지 않음 (사용자가 손으로 옮긴 위치를 덮어쓰지 않기 위함).
