# 카드뉴스 디자인 시스템

`index.html`의 `buildCardMarkup()` 함수가 실제로 그리는 카드뉴스 슬라이드의 디자인 규칙을 정리한 문서입니다. **카드 디자인(색상·타이포그래피·레이아웃)을 수정할 때는 이 문서도 같이 업데이트해서, 코드와 문서가 항상 같은 내용을 말하도록 유지할 것.**

이 문서는 2026-09-10에 실제 생성된 카드 4장(표지·본문 2종·CTA)을 리뷰하고 발견된 문제를 고친 뒤 작성됐습니다.

## 누끼 따기 / 배경 제거 (2026-09-12 추가)

카드 편집 모달에서 이미지 레이어를 넣으면 "🪄 배경 제거(누끼)" 버튼이 뜬다(차트 이미지 레이어는 제외). 서버 비용이나 이미지 유출 없이 **브라우저 안에서만** 처리하도록 `@imgly/background-removal`(오픈소스, 무료)을 CDN에서 동적으로 불러와 씀 — 별도 API 키나 서버 코드가 필요 없음.

- **`removeImageLayerBackground(idx, layerId)`**: 버튼을 눌렀을 때만 `import("https://cdn.jsdelivr.net/npm/@imgly/background-removal@.../dist/index.mjs")`로 동적 로드(평소 페이지 로딩을 무겁게 만들지 않기 위함). 처리 전 원본을 `layer.srcBeforeCutout`에 백업해두고, 결과(투명 배경 PNG)로 `layer.src`를 덮어씀. `state.cardEditor.bgRemoval = {layerId, message}`로 처리 중 상태를 표시하고, 끝나면 `null`로 되돌림.
- **`revertImageLayerCutout(idx, layerId)`**: `srcBeforeCutout`이 있으면 "↩️ 원본으로 되돌리기" 버튼이 뜨고, 누르면 원본으로 복원.
- **import map 필수**: `@imgly/background-removal`은 내부적으로 `import("onnxruntime-web")`을 실행 시점에 동적으로 호출하는데, 이건 npm 패키지 이름(bare specifier)이라 브라우저가 번들러 없이는 못 찾음. 그래서 `<head>`에 `<script type="importmap">`으로 `onnxruntime-web` → `https://cdn.jsdelivr.net/npm/onnxruntime-web@1.21.0/dist/ort.bundle.min.mjs`를 매핑해둠. **이 importmap을 지우거나 순서를 다른 모듈 스크립트 뒤로 옮기면 배경 제거가 바로 깨짐** — import map은 문서 안에서 처음 모듈이 로드되기 전에 등록돼야 브라우저가 인식하므로, 반드시 `<head>` 맨 위쪽, 다른 `<script>`보다 먼저 있어야 함.
- 모델은 가장 작은 `isnet_quint8`(~40MB)를 씀 — 다운로드 부담을 줄이는 대신 화질이 최상급 모델보다 살짝 떨어질 수 있음. 최초 1회만 느리고(수십 초~1분), 이후엔 브라우저 캐시에 남아서 빨라짐(이미지마다 매번 다시 받지 않음).
- 사람이든 사물이든 문서든 범용으로 인식하는 salient-object 모델(isnet)이라 "제품 사진 배경 지우기"나 "인물 사진 배경 지우기" 둘 다 어느 정도 동작함 — 다만 배경과 전경 경계가 애매한 사진(예: 배경과 색이 비슷한 물체)은 결과가 깔끔하지 않을 수 있음.
- **가장자리 잔여 테두리(색 번짐) 제거 — `decontaminateCutoutEdges()` (2026-09-12 추가)**: isnet 계열 모델은 트라이맵 없이 알파 마스크를 한 번에 뽑기 때문에, 마스크 경계가 실제 물체 경계와 정확히 안 맞아서 배경 픽셀 한두 줄이 그대로 "전경"으로 딸려 나오는 경우가 있음 — 이게 사용자가 보고한 "누끼 딴 사진에 테두리 선이 남는" 현상. `removeImageLayerBackground()`가 결과 PNG를 `layer.src`에 넣기 전에 이 함수를 거쳐서, 알파 채널에 최소값 필터(침식)를 걸어 그 오염된 가장자리를 통째로 지우고, 침식으로 생긴 딱딱한 경계는 1px 블러로 다시 부드럽게 만듦. 침식 반경은 이미지 긴 변 기준으로 스케일링(`round(maxDim/300)+1`, 2~7px 사이) — 이미지가 클수록 마스크가 실제 크기로 업스케일되면서 오염 테두리도 같이 두꺼워지기 때문. 합성 테스트(불투명 컬러 링을 인위적으로 만들어 침식 전/후 비교)로 완전히 제거되는 것 확인함. 반경을 더 키우면 아주 가는 디테일(머리카락 끝 등)이 살짝 깎여나갈 수 있어서, 이 부분은 "테두리 완전 제거"와 "디테일 보존" 사이의 트레이드오프임 — 나중에 특정 사진에서 디테일이 과하게 깎인다는 피드백이 오면 반경 공식을 줄이는 쪽으로 조정할 것.

## 이미지 보관함 (2026-09-12 추가)

카드 편집 모달의 "🔎 이미지 검색해서 넣기"/"🔎 이미지 검색해서 배경으로" 패널 안에 [검색]/[📁 보관함] 탭을 추가해서, 자주 쓰는 이미지를 저장해두고 다시 꺼내 쓸 수 있게 했음.

- `state.imageLibrary` — `[{id, dataUrl, name, addedAt}]`. `mkt:imageLibrary` 키로 `window.storage`(팀 공용 KV)에 저장되므로 팀 전체가 같은 보관함을 공유함(다른 브랜드 설정과 동일한 저장 방식).
- 보관함에 넣는 두 가지 경로: (1) Openverse 검색 결과 썸네일의 💾 버튼 → `tryConvertToDataURL`로 dataURL 변환 후 저장, (2) 보관함 탭의 "+ 내 이미지 업로드해서 보관함에 추가" → 로컬 파일을 그대로 저장. 둘 다 `addToImageLibrary(dataUrl, name)`을 거침.
- **저장 시 `downscaleImageForLibrary()`로 긴 변 960px, JPEG 품질 0.82로 압축**해서 KV 용량을 아낌 — 실제 슬라이드에 삽입되는 원본 이미지(`makeImageLayer` 등)는 압축하지 않음, 보관함 저장본만 줄인다. 개수는 최대 40개(`IMAGE_LIBRARY_MAX_ITEMS`)로 캡을 걸고 넘치면 오래된 것부터 버림.
- 보관함 탭에서 썸네일을 누르면 검색 결과를 고를 때와 동일하게 현재 슬라이드의 이미지 레이어 또는 배경으로 바로 적용됨. 각 썸네일의 ✕ 버튼은 `stopPropagation()`으로 클릭이 상위 pick 핸들러로 안 새게 막아둠 — 새 오버레이 버튼(저장/삭제)을 추가할 때는 항상 이 패턴을 따를 것.

## 킥커·슬라이드 번호·사무소명·전화번호도 전부 레이어 (2026-09-11 추가)

예전엔 상단 라벨(킥커), 슬라이드 번호("01","02"), 하단 사무소명·전화번호가 `brand`(전역 `state.brandSettings`)에서 매번 계산되어 그려지는 고정 요소라 슬라이드별로 따로 편집(이동·수정·삭제)할 수 없었음. 사용자가 "모든 요소를 편집할 수 있어야 한다"고 요청해서, 이 넷을 전부 일반 텍스트 레이어로 바꿨음:

- `makeChromeLayers(idx, total, brand)` — 슬라이드 역할(표지/본문/CTA)에 맞는 킥커·페이지번호·푸터 레이어 배열을 만들어주는 헬퍼. `generateCardNewsScript()`가 슬라이드를 새로 만들 때 호출해서 `state.brandSettings`의 현재 값을 초기 텍스트로 채워 넣는다.
- 레이어 타입: `kicker`, `pagenum`(표지·CTA는 없음), `footer-org`, `footer-phone`(CTA에만 있음). 전부 `buildCardMarkup()`의 범용 텍스트 렌더링 경로를 그대로 타서 드래그·인라인 편집·글자 크기 조절이 자동으로 됨. 삭제 버튼은 `custom` 타입과 같은 조건으로 묶어서 보여줌(카드 편집 화면 참고).
- **`ensureSlideLayers()`가 이전에 만들어진(레이어가 없던) 슬라이드에 이 네 레이어를 자동으로 채워 넣는다** — `slide.layers`에 kicker/pagenum/footer-org/footer-phone 중 아무거나 하나라도 있으면 건드리지 않음(사용자가 의도적으로 지운 걸 되살리지 않기 위함). 새 레이어 타입을 또 추가하게 되면 이런 이관 로직이 필요한지 항상 검토할 것.
- **"🎨 카드뉴스 설정 → 킥커·발행주체 일괄 수정" 버튼**은 이제 `state.brandSettings`만 바꾸는 게 아니라, 지금 화면의 카드뉴스 안 모든 슬라이드의 kicker/footer-org/footer-phone 레이어 텍스트도 새 값으로 덮어쓴다. 이게 "전역으로 통일"을 실제로 눈에 보이게 만드는 유일한 방법임 — 슬라이드마다 따로 값을 갖게 됐으니, 전역 설정을 바꾸는 것만으로는 이미 만들어진 카드에 반영되지 않는다.
- 상단 킥커 옆 가로선, CTA 슬라이드 하단 사무소명 위 가로선은 순수 장식이라 레이어로 만들지 않고 고정 위치에 그대로 둠 — 킥커 레이어가 자유롭게 움직여도 선은 따라가지 않는다(의도적 단순화).
- 이 때문에 `applyDynamicFlow()`의 `footerTopLimit` 계산이 더 이상 `[data-role="footer"]` 요소를 못 찾아서(그 요소 자체가 없어짐) 항상 기본값(`canvasRect.height*0.86`)으로 폴백함 — 실제 푸터 레이어 위치(y≈93%)보다 더 위쪽의 보수적인 값이라 겹침 문제는 없지만, 정확도는 살짝 떨어짐. 문제 되면 나중에 손볼 것.

## 분량(density) 토큰 예산 (2026-09-11 수정)

"컴팩트"를 선택해도 대본 초안(구성안)은 짧게 나오는데 완성된 카드뉴스는 다시 길어지는 버그가 있었음. 원인은 `generateCardNewsScript()`의 `maxTokens`이 분량(density)과 무관하게 고정값(`count*320`)이었던 것 — 토큰 여유가 있으면 모델이 그 공간을 채우려는 경향이 있어서 "컴팩트" 프롬프트 지시를 줘도 잘 안 지켜졌음. 지금은 분량별로 토큰 예산 자체를 다르게 줌: compact `count*160`(최소 900), medium `count*300`(최소 2200), full `count*420`(최소 2800), 전부 최대 8000. **분량 관련 프롬프트를 고칠 때는 토큰 예산도 같이 맞춰야 함** — 프롬프트 지시만으로는 모델이 확보된 공간을 다 안 채우도록 강제하기 어려움.

## HIGHLIGHT는 완전히 선택 사항 (2026-09-11 수정)

예전엔 CTA(마지막) 슬라이드에 "가능하면 꼭 HIGHLIGHT 한 줄 넣어"라고 강제했었는데, 사용자가 매번 강조 문구가 들어가는 걸 원하지 않아서 제거함. 지금은 HIGHLIGHT가 다른 슬라이드와 똑같이 완전히 선택 사항 — AI가 강조할 만한 내용이 없으면 그냥 안 씀. 사용자가 카드 편집 화면의 "강조 문구" 입력창으로 언제든 직접 추가할 수 있음.

## 카테고리 시스템 없음 (2026-09-10 제거)

예전엔 "개인회생"·"형사사건" 두 카테고리가 코드에 고정되어 있었고, 카테고리마다 별도 색상·AI 페르소나(광고 규정 문구)를 가질 수 있었음. 사용자가 개인회생·형사사건이라는 좁은 소재를 벗어나 "돈 안 잃는 법" 같은 더 넓은 금융 콘텐츠(돈 버는 법·사기 대처법 등)를 자유롭게 다루길 원해서 **카테고리 개념 자체를 완전히 제거**함:

- `state.pipeline`/`state.cardPipeline`에 더 이상 `category` 필드가 없음. `CATEGORY_LABEL`, `categoryPersonaPrompt(category)`도 삭제됨.
- AI 페르소나는 `writingPersonaPrompt()` 하나로 통일 — 특정 카테고리 규정이 아니라 "결과·수익 보장 표현 금지, 과장 광고 금지" 같은 범용 광고 규정 준수 원칙만 항상 적용하고, `state.styleGuide`(글쓰기 스타일 가이드)로 사용자가 원하는 톤·주제를 자유롭게 지정함.
- 색상도 카테고리별이 아니라 **단일 팔레트**(`state.cardColorPresets`, 카테고리 키 없이 flat)로 통일됨.
- 새 "테마" 개념을 다시 추가하고 싶어지면, 카테고리처럼 코드에 고정된 옵션을 또 만들지 말 것 — `styleGuide`/`cardNewsPrompt` 같은 자유 텍스트 필드로 해결하는 게 이 프로젝트의 방향과 맞음.

## 톤·말투는 항상 사용자가 지정 (2026-09-10 수정)

예전엔 `writingPersonaPrompt()`와 카드뉴스 생성 프롬프트에 "차분하고 단정한 어조를 유지해"가 못박혀 있어서, `state.styleGuide`나 `state.cardNewsPrompt`에 캐주얼한 톤(반말, 비속어 섞기 등)을 적어도 AI가 씹어버리는 문제가 있었음. 지금은 톤 관련 지시를 전부 빼고, **문체와 무관하게 지켜야 하는 것만** 하드코딩해뒀음(결과 보장 표현 금지, 원문에 없는 수치 지어내지 않기, 허위 급박함 강요 금지, 광고규정 위반 표현 금지). 말투 자체(정중함/캐주얼함/비속어 등)는 전적으로 사용자가 "글쓰기 스타일 가이드"나 "카드뉴스 스타일 프롬프트"에 적은 대로 따르게 되어 있음. **이 톤 관련 지시를 다시 하드코딩해서 못박지 말 것** — 사용자가 명시적으로 이걸 자유롭게 열어달라고 요청했음.

참고: 실제로 비속어·캐주얼한 톤이 들어간 광고가 대한변호사협회 광고규정(품위유지 요건 등)에 걸릴 수 있다는 점은 사용자에게 안내했지만, 코드로 막지는 않기로 함 — 사용자의 판단 영역.

## 글쓰기 스타일 설정 패널은 칼럼 페이지에만 있음 (2026-09-10)

`state.styleGuide`는 칼럼·카드뉴스 양쪽에 공통으로 적용되는 전역 설정이지만, 편집 UI(⚙️ 글쓰기 스타일 설정 패널)는 **칼럼 재구성하기 페이지에만** 있음 — 카드뉴스 페이지에는 이미 "🎨 카드뉴스 설정" 안에 카드뉴스 전용 "카드뉴스 스타일 프롬프트"가 있어서, 같은 종류의 설정 UI를 두 페이지에 중복으로 둘 필요가 없다고 판단해서 카드뉴스 페이지 쪽 패널은 제거함(사용자 요청).

## 색상 토큰 (`defaultCardColors()`, index.html)

`state.cardColorPresets`는 카테고리 구분 없이 만드는 모든 카드뉴스에 공통으로 적용되는 단일 팔레트입니다. 기본값은 아래와 같습니다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `accent` | `#06C8E3` (Primary Accent Cyan) | 강조 바, 슬라이드 번호, 불릿 점, CTA 보조 텍스트 |
| `coverBg` / `ctaBg` | `#121C28` (Dark Navy) | 표지·CTA 슬라이드 배경 — 이 둘은 항상 같은 색이어야 함 (스토리의 시작·끝을 시각적으로 묶는 장치) |
| `coverText` / `ctaText` | `#ffffff` | 표지·CTA의 기본 글자색 |
| `contentBg` | `#ffffff` | 본문 슬라이드 배경 |
| `contentText` | `#121B27` | 본문 슬라이드 기본 글자색 |

보조(Secondary) 텍스트색은 고정값이 아니라 배경 밝기에 따라 자동 계산됩니다 (`isDarkColor(bg)` 기준): 어두운 배경이면 `rgba(255,255,255,0.68)`, 밝은 배경이면 `#515E73`.

## 타이포그래피

- 기본 폰트: **Pretendard** (`'Pretendard','Noto Sans KR',sans-serif`). 사용자가 레이어별로 다른 폰트로 바꿀 수 있지만 기본은 항상 Pretendard.
- 전화번호·연락처처럼 숫자 데이터에는 **IBM Plex Mono** (`'Pretendard','IBM Plex Mono',monospace`) — CTA 박스 아래 전화번호, 푸터 전화번호.
- 폰트 크기는 카드 가로폭(`w`, 기본 1080px) 기준 비율로 계산: 헤드라인은 표지 `w*0.075`, 본문 `w*0.058`, CTA `w*0.062`; 보조설명 `w*0.032`; 강조 문구(HIGHLIGHT) `w*0.038`(본문)/`w*0.042`(CTA 박스); 푸터 `w*0.026`.
- **모든 한글 텍스트 레이어에는 `word-break: keep-all`이 반드시 있어야 함.** 이게 없으면 브라우저가 음절 단위로 줄바꿈해서 "사라진다면?"이 "사라진 / 다면?"처럼 단어 중간에서 끊긴다 (2026-09-10에 발견하고 고친 버그 — 다시 생기지 않게 주의).

## 레이아웃

- 캔버스 기준 크기: **1080×1350px** (4:5, 인스타그램 카드뉴스 표준 비율).
- 안전 여백(pad): `w * 80/1080` (약 7.4%) — 상하좌우 공통.
- 콘텐츠 텍스트 박스 너비: `w * 0.84` (여백 제외 전체 너비의 84%).
- 콘텐츠 시작 위치(세로): 표지 `H*0.19`, 본문 `H*0.185`, CTA `H*0.30` — 이 지점부터 헤드라인이 시작됨.
- 본문 슬라이드는 헤드라인 위에 2자리 슬라이드 번호("01", "02"...)를 accent 색으로 고정 표시 (표지·CTA는 번호 없음).
- 보조설명이 2줄 이상이면 원형 점 리스트로 렌더링되고, 1줄이면 그냥 텍스트로 렌더링됨 — 이 분기 규칙은 유지할 것.

## 다중 항목 스타일 (2026-09-10 수정: 색 박스 → 구분선)

예전엔 보조설명이 2줄 이상이면 항상 색이 채워진 둥근 박스(배경 + 패딩)로 감쌌는데, 슬라이드 8장 중 대부분이 이 박스 형태가 되면서 덱 전체가 단조롭게 느껴진다는 피드백을 받고 바꿈. 지금은:

- 항목 사이에 **얇은 구분선만** 넣음 (마지막 항목 아래는 선 없음). 배경색·둥근 테두리·바깥 패딩 전부 없음.
- 구분선 색은 `isDarkColor(bg)` 기준으로 어두운 배경이면 `rgba(255,255,255,0.14)`, 밝은 배경이면 `rgba(18,28,40,0.1)`.
- 원형 점 불릿은 그대로 유지 (박스가 아니라 리스트 마커라서 문제 없음).
- 생성 프롬프트(`generateCardNewsScript`)에도 "모든 슬라이드를 항목 나열식으로 통일하지 말고, 한 문단으로 자연스럽게 풀어 쓰는 슬라이드도 섞어라"는 지침을 추가함 — 스타일뿐 아니라 콘텐츠 구조 자체도 슬라이드마다 다양하게 나오도록.
- **다시 색이 채워진 박스 스타일로 되돌리지 말 것** — 이건 사용자가 명시적으로 별로라고 한 부분임.

## CTA 슬라이드 규칙 (2026-09-10 수정)

CTA(마지막) 슬라이드는 **행동 유도 요소가 정확히 하나**여야 합니다:

- 흰 박스 안 문구 = `slide.highlight`(AI가 만든 강조 문구)가 있으면 그걸 쓰고, 없으면 브랜드 태그라인(`brand.tagline`)을 씀. 이게 유일한 "행동을 촉구하는" 문장.
- 박스 바로 아래 보조 줄 = **항상 전화번호**(`brand.phone`), accent색 모노스페이스. 절대로 태그라인이나 또 다른 CTA 문구를 넣지 말 것 — 박스 문구와 겹쳐서 "행동 유도가 두 개"처럼 보이는 게 2026-09-10에 발견된 버그였음.
- 푸터에 사무소명+전화번호가 또 나오는 건 의도된 반복(연락처 강화)이라 문제 없음 — 겹치면 안 되는 건 "행동 유도 문구" 두 개가 나란히 있는 경우뿐임.

## 세로 여백 규칙 (2026-09-10 수정)

예전엔 콘텐츠가 항상 카드 상단의 고정된 지점(`contentTop`)에서 시작해서, 텍스트가 적은 슬라이드는 카드 하단 40~60%가 그냥 비어보였음. 지금은 `computeInitialSlideLayout()`이 역할별로 "사용 가능한 밴드"(bandTop~bandBottom)를 정해두고, 그 안에서 헤드라인+본문 블록을 **세로로 중앙 정렬**함:

- 표지: bandTop `H*0.19` ~ bandBottom `H*0.78`
- 본문: bandTop `H*0.185` ~ bandBottom `H*0.75`
- CTA: bandTop `H*0.30` ~ bandBottom `H*0.57` (그 아래 흰 CTA 박스가 `h*0.62`에서 시작하므로 겹치지 않게 여유를 둠)

블록이 밴드보다 짧으면(텍스트 적음) 아래로 내려가 밴드 중앙에 오고, 블록이 밴드보다 길면(텍스트 많음) bandTop에서 그냥 시작함(넘치지 않게). **이 밴드 경계값을 벗어나게 하드코딩하지 말고, 새 역할(예: 새로운 슬라이드 타입)을 추가할 때도 반드시 bandTop/bandBottom 쌍으로 정의할 것.**

이 로직은 슬라이드를 **새로 생성할 때만** 적용됨 — 이미 저장된 프로젝트의 기존 슬라이드는 영향받지 않음 (사용자가 손으로 옮긴 위치를 덮어쓰지 않기 위함).

## "이미지·차트 넣을 자리 남기기" 체크 → 실제로 카드에 나타나는 것 (2026-09-10 추가)

예전엔 대본 초안 단계에서 이 체크박스를 켜도 텍스트가 조금 짧아지는 것 말고는 카드에 아무 시각적 차이가 없어서 "체크해도 뭐가 다른지 모르겠다"는 피드백을 받았음. 지금은 `generateCardNewsScript()`가 완성 단계에서 체크된 슬라이드마다 실제로 레이어를 하나 더 추가함:

1. AI에게 그 슬라이드 내용 안에 **원문에 실제로 있는** 항목별 비교 값·조건이 2개 이상 있으면 `TABLE: 항목1=값1 | 항목2=값2` 형식으로 뽑아달라고 요청함 (없는 값을 지어내는 건 금지 — 프롬프트에 명시). 값이 파싱되면 `type:"table"` 레이어를 자동으로 만들어서 넣음 (see 아래).
2. TABLE 데이터가 없으면(그런 실제 데이터가 원문에 없는 경우) `type:"image"`인데 `src:null`인 **자리표시자 레이어**를 넣음 — 카드에 점선 박스 + 🖼️ 아이콘 + "이미지·차트 자리" 문구가 실제로 보임. 사용자가 나중에 "+ 이미지 추가"나 이미지 검색으로 진짜 이미지를 넣고 이 자리표시자는 지우면 됨.
3. 두 경우 모두 `computeInitialSlideLayout()`이 `hasVisual` 여부를 받아서 텍스트 밴드(`bandBottom`)를 미리 줄여두고, 그 아래 남는 공간(`visualY`/`visualHFrac`)에 표/자리표시자를 배치함. **텍스트 레이아웃과 이미지·표 자리 배치는 이렇게 서로 얽혀있으니, 둘 중 하나만 따로 고치면 겹침 버그가 재발하기 쉬움 — 항상 같이 확인할 것.**

### "table" 레이어 타입

`buildCardMarkup()`에 새로 추가된 레이어 타입. `{ id, type:"table", rows:[{label,value}], x, y, scale }` 형태. 렌더링은 다중 항목 보조설명과 같은 구분선 스타일(색 채워진 박스 아님) — 좌측에 라벨(굵게, 기본 글자색), 우측에 값(IBM Plex Mono, accent색). 카드 편집 모달에서 항목 추가/삭제/값 수정이 가능하지만, 폰트·색·정렬 툴바와 이미지 리사이즈 손잡이는 표에는 적용되지 않음(의도적으로 제외함 — 표 렌더링이 그 속성들을 쓰지 않아서 눌러도 아무 효과 없는 죽은 컨트롤이 되는 걸 막기 위함).
